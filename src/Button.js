import React from "react";
import ReactDOM from 'react-dom';

const Button = () => {
    return (
        <div>
            <button onClick={this.props.chengBack}>back</button>
        </div>
    )
}

export default Button;